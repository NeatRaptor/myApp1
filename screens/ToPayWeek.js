import React,{Component} from 'react';
import { Text, View, StyleSheet, TouchableOpacity } from 'react-native';

export default class ToPayWeek extends Component {
  render() {
    return(
      <Text>To pay by this week</Text>
    )
  }
}